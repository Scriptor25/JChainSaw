@main: num {
    ifstream input = ifstream("csaw/aoc23/input/day3.txt");
    if (!input.open())
        ret -1;

    for (str line = input.readLine(); line; line = input.readLine())
        out("%s%n", line);
    
    ret 0;
}

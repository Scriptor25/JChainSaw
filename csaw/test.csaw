# the fibonacci sequence for n #
@fib: num (n: num) {
    if (n == 0) ret 0;
    if (n == 1) ret 1;
    ret fib(n - 1) + fib(n - 2);
}

# the program entry point #
@main: num {
    ret fib(10); ## fib of 10
}
